/**
 * Legacy config.js file - functionality has been split into modular files:
 * 
 * - config-core.js: Core constants and utilities
 * - config-formatters.js: Formatting functions and option generators
 * - config-schedules.js: Schedule management
 * - config-sorts.js: Sort management
 * - config-rules.js: Rule management
 * - config-playlists.js: Playlist CRUD operations
 * - config-filters.js: Filtering and search
 * - config-bulk-actions.js: Bulk actions
 * - config-api.js: API calls
 * - config-init.js: Initialization and event listeners
 * 
 * This file is kept for backward compatibility but is now empty.
 * All functionality has been moved to the modular files above.
 */
(function () {
    'use strict';
    
    // Empty - all functionality has been moved to modular files
    // This file is kept registered in Plugin.cs and config.html for backward compatibility

})();
